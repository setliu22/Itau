# Protected Original Datasets

This directory contains the original `train`, `test`, and `validate` datasets.
Codex must treat those files as read-only. Only the human user may delete or
replace them.
