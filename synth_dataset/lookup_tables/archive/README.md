# Archived Lookup Tables

Old Q25 and exploratory lookup artifacts can be parked here while the project
uses the simpler validation replacement pipeline. This directory is not an
active input unless a script is explicitly pointed at a file inside it.
